# Core dependencies
requests>=2.25.0
colorama>=0.4.4
pytz>=2021.1
bs4>=0.0.1  # BeautifulSoup
lxml>=4.6.3

# Python 2/3 compatibility (for older Python versions)
six>=1.15.0

# Optional GUI support
# Uncomment if you want to use the GUI features
# tkinter  # Built-in for Python 3, for Python 2 install python-tk 